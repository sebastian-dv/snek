(let ((x 1) (y 2))
  (block
    (set! x (+ x y))
    (let ((z (add1 x)))
      (if (> z 3)
        (loop (if (< x 10)
                  (block (set! x (+ x 1)) (break x))
                  (break x)))
        (sub1 z)))))
